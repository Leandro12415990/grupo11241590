Algoritmo número feliz
ED: n, aux, total <- 0, verificacao, i <- 0 inteiro

Inicio

	escrever("Insira o número:")
	ler(n)
	repetir até total = 1 ou i = 20
		aux <- n // 10
		total <- total + (aux * aux)
		aux <- n // 100
		total <- total + (aux * aux)
		n <- total
		i <- i + 1
	Fim repetir
	
	se n = 1 então
		esrever("true")
	senão
		escrever("false")
	então
	escrever("O resultado é: ", n)
	
Fim